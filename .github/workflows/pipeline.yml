name: Pipeline

on:
  workflow_dispatch:
    inputs:
      feature_id:
        description: "Feature ID to process"
        required: true

jobs:
  setup:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Create feature branch if needed
        run: |
          if git ls-remote --heads origin feature/${{ inputs.feature_id }} | grep -q feature/${{ inputs.feature_id }}; then
            echo "Branch exists, checking out"
            git fetch origin feature/${{ inputs.feature_id }}
            git checkout feature/${{ inputs.feature_id }}
          else
            echo "Creating new branch"
            git checkout -b feature/${{ inputs.feature_id }}
            git push -u origin feature/${{ inputs.feature_id }}
          fi

  product:
    needs: setup
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/${{ inputs.feature_id }}

      - name: Check if stage already complete
        id: check_status
        run: |
          if [ -f ".ai/pipeline/${{ inputs.feature_id }}.state" ]; then
            if grep -q "product: ✓" .ai/pipeline/${{ inputs.feature_id }}.state; then
              echo "Stage already complete, skipping"
              echo "skip=true" >> $GITHUB_OUTPUT
              exit 0
            fi
          fi
          echo "skip=false" >> $GITHUB_OUTPUT

      - name: Set up Python
        if: steps.check_status.outputs.skip != 'true'
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        if: steps.check_status.outputs.skip != 'true'
        run: pip install -r requirements.txt

      - name: Run Product Agent
        if: steps.check_status.outputs.skip != 'true'
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          GOOGLE_API_KEY: ${{ secrets.GOOGLE_API_KEY }}
          AI_PROVIDER: ${{ vars.AI_PROVIDER || 'openai' }}
          MODEL: ${{ vars.MODEL || 'gpt-4.1' }}
          SAVE_JSON_ARTIFACTS: "true"
        run: |
          echo "🤖 Running Product Agent..."
          python scripts/invoke_product_agent.py ${{ inputs.feature_id }}

      - name: Upload JSON artifacts
        if: always() && steps.check_status.outputs.skip != 'true'
        uses: actions/upload-artifact@v4
        with:
          name: product-json-artifacts-${{ inputs.feature_id }}
          path: json_artifacts/
          if-no-files-found: ignore

      - name: Update pipeline state
        if: steps.check_status.outputs.skip != 'true'
        run: |
          sed -i "s/status: .*/status: product_complete/" .ai/pipeline/${{ inputs.feature_id }}.state
          sed -i "s/product: .*/product: ✓/" .ai/pipeline/${{ inputs.feature_id }}.state

      - name: Commit outputs
        if: steps.check_status.outputs.skip != 'true'
        run: |
          git config user.name "Pipeline Bot"
          git config user.email "pipeline@aid.local"
          git add product/decisions/ experiments/active.md .ai/pipeline/
          git commit -m "Product: ${{ inputs.feature_id }} analysis complete" || echo "No changes"
          git push

  design:
    needs: product
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/${{ inputs.feature_id }}

      - name: Check if stage already complete
        id: check_status
        run: |
          if [ -f ".ai/pipeline/${{ inputs.feature_id }}.state" ]; then
            if grep -q "design: ✓" .ai/pipeline/${{ inputs.feature_id }}.state; then
              echo "Stage already complete, skipping"
              echo "skip=true" >> $GITHUB_OUTPUT
              exit 0
            fi
          fi
          echo "skip=false" >> $GITHUB_OUTPUT

      - name: Set up Python
        if: steps.check_status.outputs.skip != 'true'
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        if: steps.check_status.outputs.skip != 'true'
        run: pip install -r requirements.txt

      - name: Check if design needed
        if: steps.check_status.outputs.skip != 'true'
        id: check-design
        run: |
          NEEDS_DESIGN=$(grep "needs_design:" .ai/pipeline/${{ inputs.feature_id }}.state | cut -d: -f2 | xargs || echo "false")
          echo "needs_design=$NEEDS_DESIGN" >> $GITHUB_OUTPUT
          if [ "$NEEDS_DESIGN" = "false" ]; then
            echo "⏭️ Skipping Design Agent (not needed)"
          fi

      - name: Run Design Agent
        if: steps.check_status.outputs.skip != 'true' && steps.check-design.outputs.needs_design == 'true'
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          GOOGLE_API_KEY: ${{ secrets.GOOGLE_API_KEY }}
          AI_PROVIDER: ${{ vars.AI_PROVIDER || 'openai' }}
          MODEL: ${{ vars.MODEL || 'gpt-4.1' }}
          SAVE_JSON_ARTIFACTS: "true"
        run: |
          echo "🎨 Running Design Agent (Iterative Mode)..."
          python scripts/invoke_design_agent_iterative.py ${{ inputs.feature_id }}

      - name: Upload JSON artifacts
        if: always() && steps.check_status.outputs.skip != 'true'
        uses: actions/upload-artifact@v4
        with:
          name: design-json-artifacts-${{ inputs.feature_id }}
          path: json_artifacts/
          if-no-files-found: ignore

      - name: Update pipeline state
        if: steps.check_status.outputs.skip != 'true' && steps.check-design.outputs.needs_design == 'true'
        run: |
          sed -i 's/status:.*/status: design_complete/' .ai/pipeline/${{ inputs.feature_id }}.state
          sed -i 's/design: .*/design: ✓/' .ai/pipeline/${{ inputs.feature_id }}.state

      - name: Commit outputs
        if: steps.check_status.outputs.skip != 'true' && steps.check-design.outputs.needs_design == 'true'
        run: |
          git config user.name "Pipeline Bot"
          git config user.email "pipeline@aid.local"
          git add design/ .ai/pipeline/
          git commit -m "Design: ${{ inputs.feature_id }} spec complete" || echo "No changes"
          git push

  architect:
    needs: design
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/${{ inputs.feature_id }}

      - name: Check if stage already complete
        id: check_status
        run: |
          if [ -f ".ai/pipeline/${{ inputs.feature_id }}.state" ]; then
            if grep -q "architect: ✓" .ai/pipeline/${{ inputs.feature_id }}.state; then
              echo "Stage already complete, skipping"
              echo "skip=true" >> $GITHUB_OUTPUT
              exit 0
            fi
          fi
          echo "skip=false" >> $GITHUB_OUTPUT

      - name: Set up Python
        if: steps.check_status.outputs.skip != 'true'
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        if: steps.check_status.outputs.skip != 'true'
        run: pip install -r requirements.txt

      - name: Run Architect Agent
        if: steps.check_status.outputs.skip != 'true'
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          GOOGLE_API_KEY: ${{ secrets.GOOGLE_API_KEY }}
          AI_PROVIDER: ${{ vars.AI_PROVIDER || 'openai' }}
          MODEL: ${{ vars.MODEL || 'gpt-4.1' }}
          SAVE_JSON_ARTIFACTS: "true"
        run: |
          echo "🏗️ Running Architect Agent..."
          python scripts/invoke_architect_agent.py ${{ inputs.feature_id }}

      - name: Upload JSON artifacts
        if: always() && steps.check_status.outputs.skip != 'true'
        uses: actions/upload-artifact@v4
        with:
          name: architect-json-artifacts-${{ inputs.feature_id }}
          path: json_artifacts/
          if-no-files-found: ignore

      - name: Update pipeline state
        if: steps.check_status.outputs.skip != 'true'
        run: |
          sed -i 's/status:.*/status: architect_complete/' .ai/pipeline/${{ inputs.feature_id }}.state
          sed -i 's/architect: .*/architect: ✓/' .ai/pipeline/${{ inputs.feature_id }}.state

      - name: Commit outputs
        if: steps.check_status.outputs.skip != 'true'
        run: |
          git config user.name "Pipeline Bot"
          git config user.email "pipeline@aid.local"
          git add design/architecture/ design/technical-specs/ .ai/pipeline/
          git commit -m "Architect: ${{ inputs.feature_id }} review complete" || echo "No changes"
          git push

  dev:
    needs: architect
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/${{ inputs.feature_id }}

      - name: Check if stage already complete
        id: check_status
        run: |
          if [ -f ".ai/pipeline/${{ inputs.feature_id }}.state" ]; then
            if grep -q "dev: ✓" .ai/pipeline/${{ inputs.feature_id }}.state; then
              echo "Stage already complete, skipping"
              echo "skip=true" >> $GITHUB_OUTPUT
              exit 0
            fi
          fi
          echo "skip=false" >> $GITHUB_OUTPUT

      - name: Call Dev Service
        if: steps.check_status.outputs.skip != 'true'
        run: |
          echo "🚀 Calling Dev Service for feature: ${{ inputs.feature_id }}"

          # Call dev service
          RESPONSE=$(curl -s -w "\n%{http_code}" -X POST ${{ secrets.DEV_SERVICE_URL }}/dev-request \
            -H "Content-Type: application/json" \
            -H "Authorization: Bearer ${{ secrets.DEV_SERVICE_TOKEN }}" \
            -d "{
              \"feature_id\": \"${{ inputs.feature_id }}\",
              \"branch\": \"feature/${{ inputs.feature_id }}\",
              \"repository\": \"${{ github.repository }}\"
            }")

          HTTP_CODE=$(echo "$RESPONSE" | tail -n1)
          BODY=$(echo "$RESPONSE" | sed '$d')

          echo "Response code: $HTTP_CODE"
          echo "Response body: $BODY"

          # Check if request was accepted
          if [ "$HTTP_CODE" = "202" ]; then
            echo "✅ Dev service accepted the request"
          else
            echo "❌ Dev service rejected the request (HTTP $HTTP_CODE)"
            exit 1
          fi

      - name: Wait for Dev Service to complete
        if: steps.check_status.outputs.skip != 'true'
        run: |
          echo "⏳ Waiting for dev service to complete implementation..."

          MAX_WAIT=1800  # 30 minutes
          ELAPSED=0
          INTERVAL=30

          while [ $ELAPSED -lt $MAX_WAIT ]; do
            # Pull latest changes from feature branch
            git pull origin feature/${{ inputs.feature_id }} || true
            
            # Check if dev service has committed changes
            if git log --since="1 minute ago" --author="Dev Service Bot" --oneline | grep -q "Feature ${{ inputs.feature_id }}"; then
              echo "✅ Dev service completed - new commit detected"
              break
            fi
            
            # Alternative: Check for completion marker
            if [ -f ".ai/pipeline/${{ inputs.feature_id }}.dev-complete" ]; then
              echo "✅ Dev service marked as complete"
              break
            fi
            
            echo "Still waiting... (${ELAPSED}s / ${MAX_WAIT}s)"
            sleep $INTERVAL
            ELAPSED=$((ELAPSED + INTERVAL))
          done

          if [ $ELAPSED -ge $MAX_WAIT ]; then
            echo "⏰ Timeout waiting for dev service"
            echo "Check dev service logs for errors"
            exit 1
          fi

      - name: Pull final changes
        if: steps.check_status.outputs.skip != 'true'
        run: |
          git pull origin feature/${{ inputs.feature_id }}

      - name: Validate build commands exist
        if: steps.check_status.outputs.skip != 'true'
        id: build_validation
        run: |
          BUILD_FILE=".ai/pipeline/${{ inputs.feature_id }}.build.json"

          if [ ! -f "$BUILD_FILE" ]; then
            echo "⚠️  No build file generated by dev service"
            echo "Dev service may have failed"
            exit 1
          fi

          echo "✅ Build commands file found"
          cat "$BUILD_FILE"

          echo "validation_passed=true" >> $GITHUB_OUTPUT

      - name: Update pipeline state
        if: steps.check_status.outputs.skip != 'true'
        run: |
          sed -i 's/status:.*/status: dev_complete/' .ai/pipeline/${{ inputs.feature_id }}.state
          sed -i 's/dev: .*/dev: ✓/' .ai/pipeline/${{ inputs.feature_id }}.state
          echo "  build_validation: ✓ (via dev service)" >> .ai/pipeline/${{ inputs.feature_id }}.state

      - name: Commit outputs
        if: steps.check_status.outputs.skip != 'true'
        run: |
          git config user.name "Pipeline Bot"
          git config user.email "pipeline@aid.local"
          git add .ai/pipeline/
          git commit -m "Dev: ${{ inputs.feature_id }} stage complete (via dev service)" || echo "No changes"
          git push

  qa:
    needs: dev
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/${{ inputs.feature_id }}

      - name: Check if stage already complete
        id: check_status
        run: |
          if [ -f ".ai/pipeline/${{ inputs.feature_id }}.state" ]; then
            if grep -q "qa: ✓" .ai/pipeline/${{ inputs.feature_id }}.state; then
              echo "Stage already complete, skipping"
              echo "skip=true" >> $GITHUB_OUTPUT
              exit 0
            fi
          fi
          echo "skip=false" >> $GITHUB_OUTPUT

      - name: Validate build results
        if: steps.check_status.outputs.skip != 'true'
        id: qa_check
        run: |
          echo "🧪 Running QA validation..."

          STATE_FILE=".ai/pipeline/${{ inputs.feature_id }}.state"

          # Check if build validation passed
          if grep -q "build_validation: ✓" "$STATE_FILE"; then
            echo "✅ Build validation passed"
            echo "qa_passed=true" >> $GITHUB_OUTPUT
          elif grep -q "build_validation: ✗" "$STATE_FILE"; then
            echo "❌ Build validation failed - blocking QA approval"
            echo "qa_passed=false" >> $GITHUB_OUTPUT
            exit 1
          elif grep -q "build_validation: - skipped" "$STATE_FILE"; then
            echo "⚠️  Build validation was skipped (no build commands)"
            echo "qa_passed=true" >> $GITHUB_OUTPUT
          else
            echo "⚠️  No build validation results found, passing with warning"
            echo "qa_passed=true" >> $GITHUB_OUTPUT
          fi

          echo "✅ QA checks complete"

      - name: Update pipeline state
        if: steps.check_status.outputs.skip != 'true'
        run: |
          sed -i 's/status:.*/status: qa_complete/' .ai/pipeline/${{ inputs.feature_id }}.state
          sed -i 's/qa: .*/qa: ✓/' .ai/pipeline/${{ inputs.feature_id }}.state

      - name: Commit outputs
        run: |
          git config user.name "Pipeline Bot"
          git config user.email "pipeline@aid.local"
          git add design/validations/ .ai/pipeline/
          git commit -m "QA: ${{ inputs.feature_id }} validation complete" || echo "No changes"
          git push

  ops:
    needs: qa
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/${{ inputs.feature_id }}

      - name: Check if stage already complete
        id: check_status
        run: |
          if [ -f ".ai/pipeline/${{ inputs.feature_id }}.state" ]; then
            if grep -q "ops: ✓" .ai/pipeline/${{ inputs.feature_id }}.state; then
              echo "Stage already complete, skipping"
              echo "skip=true" >> $GITHUB_OUTPUT
              exit 0
            fi
          fi
          echo "skip=false" >> $GITHUB_OUTPUT

      - name: Set up Python
        if: steps.check_status.outputs.skip != 'true'
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        if: steps.check_status.outputs.skip != 'true'
        run: pip install openai google-genai python-dotenv

      - name: Run Ops Agent
        if: steps.check_status.outputs.skip != 'true'
        env:
          GOOGLE_API_KEY: ${{ secrets.GOOGLE_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          AI_PROVIDER: ${{ vars.AI_PROVIDER }}
          MODEL: ${{ vars.MODEL }}
        run: |
          echo "🚀 Running Ops Agent..."
          python scripts/invoke_ops_agent.py "${{ inputs.feature_id }}"

      - name: Update pipeline state
        if: steps.check_status.outputs.skip != 'true'
        run: |
          sed -i 's/status:.*/status: ops_complete/' .ai/pipeline/${{ inputs.feature_id }}.state
          sed -i 's/ops: .*/ops: ✓/' .ai/pipeline/${{ inputs.feature_id }}.state

      - name: Commit outputs
        if: steps.check_status.outputs.skip != 'true'
        run: |
          git config user.name "Pipeline Bot"
          git config user.email "pipeline@aid.local"
          git add .ai/pipeline/
          git commit -m "Ops: ${{ inputs.feature_id }} deployment ready" || echo "No changes"
          git push

      - name: Create deployment PR to main
        if: steps.check_status.outputs.skip != 'true'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh pr create \
            --base main \
            --head feature/${{ inputs.feature_id }} \
            --title "✅ Deploy: ${{ inputs.feature_id }}" \
            --body "## Feature Ready for Deployment

          All pipeline stages completed for \`${{ inputs.feature_id }}\`.

          ### 🔍 Pipeline Status
          - ✓ Product Agent: Decision & experiment defined
          - ✓ Design Agent: Specifications created
          - ✓ Architect Agent: Technical review complete
          - ✓ Dev Agent: Implementation complete
          - ✓ QA Agent: Validation passed
          - ✓ Ops Agent: Deployment ready

          ### 🚀 Deployment
          Merge this PR to deploy to main.

          _Ready for final review and deployment._" || true

          # Try to add labels if they exist (ignore if not)
          gh pr edit feature/${{ inputs.feature_id }} --add-label "deploy,awaiting-approval" 2>/dev/null || echo "Note: Labels not added (may not exist in repo)"

  handle-failure:
    needs: [product, design, architect, dev, qa, ops]
    if: failure()
    runs-on: ubuntu-latest
    permissions:
      actions: write
      contents: read
      issues: write
    steps:
      - uses: actions/checkout@v4

      - name: Create error issue and trigger fix
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          ERROR_ID="error-$(date -u +%Y%m%d-%H%M%S)"
          echo "🔴 Pipeline failed - creating issue and triggering auto-fix: $ERROR_ID"

          # Create issue
          ISSUE_URL=$(gh issue create \
            --title "🔴 Pipeline Failed: ${{ inputs.feature_id }} - $ERROR_ID" \
            --body "## Pipeline Failure

          **Feature ID:** \`${{ inputs.feature_id }}\`  
          **Error ID:** \`$ERROR_ID\`  
          **Workflow:** Pipeline  
          **Run:** ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}

          ### Failed Stage
          One or more pipeline stages failed. Check the workflow run for details.

          ### Error Recovery Agent Task
          1. Download and analyze the failed workflow logs
          2. Identify the root cause of the failure
          3. Implement a minimal fix
          4. Create a PR with the fix
          5. Update this issue with findings

          ---
          _Auto-fix will be triggered automatically._")

          ISSUE_NUMBER=$(echo $ISSUE_URL | grep -oP '\d+$')
          echo "Created issue #$ISSUE_NUMBER"

          # Try to add labels (ignore if labels don't exist)
          gh issue edit "$ISSUE_NUMBER" --add-label "pipeline-error,auto-fix" 2>/dev/null || echo "Note: Labels not added (may not exist in repo)"

          # Trigger error-fix workflow
          gh workflow run error-fix.yml \
            -f error_id="$ERROR_ID" \
            -f issue_number="$ISSUE_NUMBER"
